#include<bits/stdc++.h>
using namespace std;
int main(){
    double a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    double tong = (a + b  +c) / (d + e + f);
    cout << fixed << setprecision(1) << tong;
    return 0;
}